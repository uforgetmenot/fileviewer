version: "3.9"
services:
  fileviewer:
    build: .
    volumes:
      - ./:/workspace
    working_dir: /workspace
    environment:
      - WATCH_DIR=/workspace
    restart: unless-stopped
    tty: true
